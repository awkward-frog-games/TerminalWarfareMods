-- Sound filename mapping for the interceptcmd mod.
-- Drop matching .mp3, .wav, or .ogg files in this folder (extension optional here).
-- Missing files are skipped silently at runtime.
return {
    launch = "launch",
    explode = "explode",
    node_hit = "node_hit",
    wave_start = "wave_start",
    win = "win",
    game_over = "game_over",
}
