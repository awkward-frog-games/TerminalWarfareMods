-- Sound filename mapping for the pipedream mod.
-- Drop matching .mp3, .wav, or .ogg files in this folder (extension optional here).
-- Missing files are skipped silently at runtime.
return {
    rotate = "rotate",
    flow = "flow",
    stage_clear = "stage_clear",
    win = "win",
}
